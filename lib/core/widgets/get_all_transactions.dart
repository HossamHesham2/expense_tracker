import 'package:flutter/material.dart';

class GetAllTransactions extends StatelessWidget {
  const GetAllTransactions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
